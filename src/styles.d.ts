/**
 * The project pins an explicit `types` list, so Next's ambient stylesheet
 * declaration is not in scope. Side-effect stylesheet imports are declared
 * here instead.
 */
declare module "*.css";
