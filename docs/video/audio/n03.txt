The agent can see the tools before I've allowed anything. Reading in redacted mode tells it there's an application here and nothing else. The moment it asks for protected data, the server refuses. Consent required. The refusal is the server's, not the model's.
